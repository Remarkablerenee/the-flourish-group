:root{
  --ivory:#f5f0e7;
  --paper:#fbf8f2;
  --white:#fffdf9;
  --charcoal:#1d1d1b;
  --ink:#262522;
  --muted:#706c64;
  --line:#ded6c9;
  --gold:#c7a15a;
  --gold-dark:#9e7838;
  --orange:#e85a12;
  --serif:"Playfair Display",Georgia,serif;
  --sans:"DM Sans",Arial,sans-serif;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{margin:0;background:var(--paper);color:var(--ink);font-family:var(--sans);line-height:1.6}
a{color:inherit;text-decoration:none}
img{max-width:100%}
.section-pad{padding-left:clamp(24px,5vw,80px);padding-right:clamp(24px,5vw,80px)}
.site-header{
  position:relative;z-index:20;display:flex;align-items:center;justify-content:space-between;
  min-height:88px;padding:14px clamp(22px,5vw,72px);background:rgba(251,248,242,.94);
  border-bottom:1px solid var(--line);backdrop-filter:blur(12px)
}
.brand{display:flex;align-items:center;gap:13px}
.brand-mark{
  width:50px;height:50px;display:block;flex:0 0 50px;border:0!important;border-radius:0!important;
  background:url("assets/tfg-monogram.svg") center/contain no-repeat!important;position:relative
}
.brand-mark span{display:none!important}
.brand-name{font-size:11px;line-height:1.05;font-weight:700;letter-spacing:.18em}
.nav{display:flex;gap:28px;align-items:center;margin-left:auto;margin-right:28px}
.nav a{font-size:12px;text-transform:uppercase;letter-spacing:.12em;color:#555047;position:relative;padding:8px 0}
.nav a:after{content:"";position:absolute;left:0;bottom:0;width:0;height:2px;background:var(--orange);transition:.25s}
.nav a:hover:after,.nav a.active:after{width:100%}
.nav-cta,.btn{
  display:inline-flex;align-items:center;gap:12px;border:1px solid var(--charcoal);
  padding:13px 18px;font-size:11px;text-transform:uppercase;letter-spacing:.12em;font-weight:700;
  transition:.25s
}
.nav-cta{background:var(--charcoal);color:#fffdf9;border-color:var(--charcoal)}
.nav-cta:hover,.btn-primary:hover{background:var(--orange);border-color:var(--orange)}
.menu-toggle{display:none;background:none;border:0}
.menu-toggle span{display:block;width:25px;height:2px;background:var(--charcoal);margin:5px}

.hero{
  min-height:690px;display:grid;grid-template-columns:minmax(0,1.02fr) minmax(420px,.98fr);
  gap:clamp(30px,5vw,90px);align-items:center;position:relative;overflow:hidden;
  background:
    radial-gradient(circle at 78% 35%,rgba(199,161,90,.24),transparent 27%),
    linear-gradient(112deg,#f7f2e9 0%,#f5eee2 58%,#e9dfcf 100%);
  border-bottom:1px solid var(--line)
}
.hero:before{content:"";position:absolute;right:-9vw;top:-13vw;width:48vw;height:48vw;border:1px solid rgba(199,161,90,.35);border-radius:50%}
.hero:after{content:"";position:absolute;right:7vw;bottom:-25%;width:28vw;height:28vw;border:1px solid rgba(232,90,18,.2);border-radius:50%}
.hero-copy{position:relative;z-index:3;max-width:700px;padding-top:10px}
.eyebrow{font-size:10px;letter-spacing:.2em;font-weight:700;color:var(--orange);display:flex;align-items:center;gap:12px}
.eyebrow i{width:34px;height:1px;background:currentColor;display:inline-block}
.eyebrow.light{color:var(--gold)}
.hero h1{
  margin:25px 0 24px;font-family:var(--serif);font-weight:500;font-size:clamp(76px,9vw,138px);
  line-height:.78;letter-spacing:-.055em;color:var(--charcoal)
}
.hero h1 em,.page-title em{font-style:italic;color:var(--gold-dark);font-weight:500}
.hero-lede{max-width:570px;font-size:18px;line-height:1.65;color:#5f5a52}
.hero-actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:30px}
.btn{background:transparent}
.btn-primary{background:var(--charcoal);color:#fffdf9}
.hero-art{
  min-height:500px;position:relative;border:1px solid rgba(29,29,27,.12);
  background:linear-gradient(145deg,#282825 0%,#3c3932 55%,#8c6a38 100%);
  box-shadow:0 25px 60px rgba(50,42,29,.16);overflow:hidden
}
.hero-art:before{
  content:"";position:absolute;inset:34px;border:1px solid rgba(255,255,255,.18);
  background:linear-gradient(135deg,rgba(255,255,255,.08),transparent 50%); 
}
.hero-art:after{
  content:"WHAT'S NEXT";position:absolute;right:28px;bottom:23px;color:rgba(255,255,255,.42);
  font-size:9px;letter-spacing:.25em
}
.window-glow{position:absolute;right:13%;top:11%;width:57%;height:57%;background:linear-gradient(135deg,rgba(246,232,198,.8),rgba(232,90,18,.25));filter:blur(1px);transform:skew(-9deg)}
.city{position:absolute;left:8%;right:8%;bottom:24%;height:30%;opacity:.38;background:
  linear-gradient(90deg,transparent 0 4%,#f7f1e6 4% 8%,transparent 8% 11%,#d2b777 11% 17%,transparent 17% 22%,#f7f1e6 22% 28%,transparent 28% 33%,#b88d4b 33% 40%,transparent 40% 45%,#f7f1e6 45% 50%,transparent 50% 56%,#c7a15a 56% 63%,transparent 63% 68%,#f7f1e6 68% 74%,transparent 74% 79%,#b88d4b 79% 87%,transparent 87%);
  clip-path:polygon(0 45%,7% 30%,13% 45%,21% 10%,29% 45%,37% 22%,44% 45%,52% 0,60% 45%,68% 28%,76% 45%,84% 12%,92% 45%,100% 26%,100% 100%,0 100%)
}
.desk{position:absolute;left:0;right:0;bottom:0;height:22%;background:#171715;border-top:2px solid var(--gold)}
.laptop{position:absolute;left:21%;bottom:19%;width:58%;height:43%;background:#121211;border:5px solid #c8b99f;border-radius:10px 10px 4px 4px;box-shadow:0 16px 20px rgba(0,0,0,.35)}
.laptop:before{content:"";position:absolute;inset:11px;background:linear-gradient(135deg,#f2e6cb 0%,#d1a45d 48%,#e85a12 100%)}
.laptop:after{content:"";position:absolute;left:-9%;right:-9%;bottom:-15px;height:13px;background:#b8ab96;border-radius:0 0 12px 12px}
.notebook{position:absolute;z-index:4;left:10%;bottom:13%;width:31%;height:23%;background:#eee3d0;transform:rotate(-8deg);box-shadow:0 12px 22px rgba(0,0,0,.28);display:flex;align-items:center;justify-content:center}
.notebook:before{content:"TFG";font-family:var(--serif);font-size:33px;color:var(--charcoal);letter-spacing:.05em}
.notebook span{display:none}
.plant{position:absolute;z-index:5;right:7%;bottom:14%;width:13%;height:29%;background:var(--gold-dark);border-radius:55% 45% 15% 15%;transform:rotate(4deg);opacity:.8}
.plant:before{content:"";position:absolute;width:180%;height:100%;left:-45%;top:-62%;background:radial-gradient(ellipse at center, #c7a15a 0 16%,transparent 17%);filter:drop-shadow(16px -4px 0 #9e7838) drop-shadow(-14px 7px 0 #d1b476)}

.pillars{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;padding-top:24px;padding-bottom:24px;background:var(--paper)}
.pillar-card{background:var(--white);border:1px solid var(--line);padding:38px 34px;min-height:250px;transition:.25s}
.pillar-card:hover{transform:translateY(-4px);box-shadow:0 16px 35px rgba(44,38,27,.08)}
.pillar-card .icon{font-size:24px;color:var(--orange);margin-bottom:24px}
.pillar-card h2{font-family:var(--serif);font-size:34px;font-weight:500;margin:0 0 10px}
.pillar-card p{color:var(--muted);max-width:430px}
.pillar-card a,.feature-body a{font-size:11px;text-transform:uppercase;letter-spacing:.12em;font-weight:700;color:var(--orange)}

.statement{background:var(--charcoal);color:#f7f1e8;padding:95px 0}
.statement-inner{display:grid;grid-template-columns:1.1fr .9fr;gap:80px;align-items:end}
.statement h2{font-family:var(--serif);font-size:clamp(48px,5.5vw,82px);font-weight:500;line-height:1.02;margin:25px 0 0}
.statement h2 em{color:var(--gold);font-style:italic}
.statement-copy p{color:#c6c0b6;font-size:17px;line-height:1.75;margin-top:0}
.btn-dark{border-color:#f7f1e8;color:#f7f1e8}
.btn-dark:hover{background:var(--gold);border-color:var(--gold);color:var(--charcoal)}

.content-section{padding-top:95px;padding-bottom:95px}
.section-heading{display:flex;justify-content:space-between;align-items:end;gap:30px;margin-bottom:45px}
.section-heading h2{font-family:var(--serif);font-size:clamp(44px,5vw,68px);font-weight:500;line-height:1;margin:18px 0 0}
.section-heading>a{font-size:11px;text-transform:uppercase;letter-spacing:.1em;font-weight:700}
.feature-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.feature-card{background:var(--white);border:1px solid var(--line);overflow:hidden}
.feature-visual{height:180px;display:flex;align-items:flex-end;padding:18px;background:#24231f;position:relative;overflow:hidden}
.feature-visual:after{content:"";position:absolute;width:180px;height:180px;border:1px solid rgba(255,255,255,.3);border-radius:50%;right:-50px;top:-50px}
.visual-orange{background:linear-gradient(135deg,#d95a1c,#7c3d1f)}
.visual-warm{background:linear-gradient(135deg,#6e6251,#c7a15a)}
.visual-gold{background:linear-gradient(135deg,#c7a15a,#4e4030)}
.feature-visual span{position:relative;z-index:2;color:#fff;font-size:10px;letter-spacing:.2em;font-weight:700}
.feature-body{padding:28px 28px 30px}
.feature-body small{font-size:10px;text-transform:uppercase;letter-spacing:.14em;color:var(--orange);font-weight:700}
.feature-body h3{font-family:var(--serif);font-size:29px;font-weight:500;margin:9px 0}
.feature-body p{color:var(--muted);min-height:50px}

.growth{display:grid;grid-template-columns:1fr 1fr;gap:70px;align-items:center;background:#eee6d9;border-top:1px solid var(--line)}
.growth h2{font-family:var(--serif);font-size:clamp(48px,6vw,80px);font-weight:500;line-height:.98;margin:20px 0 0}
.growth h2 em{color:var(--orange);font-style:italic}
.growth p{max-width:540px;color:var(--muted);font-size:17px}
.btn-outline:hover{background:var(--charcoal);color:#fff}

.page-title{font-family:var(--serif);font-size:clamp(54px,7vw,100px);line-height:.95;font-weight:500;margin:28px 0}
.page-lede{font-size:19px;max-width:650px;color:var(--muted)}

.site-footer{background:#161614;color:#e8e1d5;padding:55px clamp(24px,5vw,80px);display:grid;grid-template-columns:1.2fr 1fr 1fr;gap:40px;align-items:end}
.footer-brand{display:flex;align-items:center;gap:13px;font-size:12px;letter-spacing:.14em;font-weight:700}
.footer-brand .brand-mark.small{width:42px;height:42px;flex-basis:42px}
.footer-brand small{display:block;color:var(--gold);font-size:8px;letter-spacing:.2em;margin-top:7px}
.footer-nav{display:flex;flex-wrap:wrap;gap:18px}
.footer-nav a,.footer-meta{font-size:11px;color:#aaa398}
.footer-meta{display:flex;flex-direction:column;gap:7px;text-align:right}
.footer-meta a{color:#e8e1d5}

@media(max-width:900px){
  .nav{display:none}.nav-cta{display:none}.menu-toggle{display:block}
  .hero{grid-template-columns:1fr;min-height:auto;padding-top:75px;padding-bottom:65px}
  .hero-art{min-height:420px}.pillars,.feature-grid{grid-template-columns:1fr}
  .statement-inner,.growth{grid-template-columns:1fr;gap:35px}
  .site-footer{grid-template-columns:1fr}.footer-meta{text-align:left}
}
@media(max-width:600px){
  .site-header{min-height:76px}.brand-name{font-size:9px}.brand-mark{width:44px;height:44px;flex-basis:44px}
  .hero h1{font-size:clamp(64px,18vw,96px)}.hero-art{min-height:330px}
  .section-heading{display:block}.section-heading>a{display:inline-block;margin-top:20px}
  .content-section{padding-top:65px;padding-bottom:65px}.statement{padding:70px 0}
}
